import "./Register.css";
import { RegisterForm } from "./components";

const Register = () => {
  return (
    <section className="register">
      <RegisterForm />
    </section>
  );
};

export default Register;
